# Session report — <YYYY-MM-DD> — <slug>

> Branch: `<branch>` · Step: `<step>`
> (Copy this file to `doc/sessions/<YYYY-MM-DD>-<slug>.md`; never overwrite.)

## Done

- …

## Not done / remaining

- …

## Decisions

- … (durable cross-session decisions; mirror to auto-memory if they change the plan)

## Guardrail status

Numbers from the preflight run (`pnpm precommit`):

- `pnpm build` — …
- `pnpm check` (Biome) — …
- `pnpm typecheck` — …
- `pnpm test:coverage` — … (coverage: …)
- `pnpm knip` — …

## State to resume from

**Single next action:** …

Gotchas / half-done edits / stale state:

- …
